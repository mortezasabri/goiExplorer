% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{build_extra_plots}
\alias{build_extra_plots}
\title{Build every exploratory plot for a finished run}
\usage{
build_extra_plots(
  res_output,
  outdir = res_output$directory,
  lfcCutoff = 1,
  pCutoff = 0.05,
  palette = c("#4f8832", "#f79c18"),
  n_heatmap = 40,
  n_correlated = 20,
  verbose = TRUE
)
}
\arguments{
\item{res_output}{The list returned by \code{\link[=run_pipeline]{run_pipeline()}} / \code{\link[=pipeline]{pipeline()}}.}

\item{outdir}{Character or \code{NULL}. Directory to write PNGs into. Defaults to
\code{res_output$directory}; pass \code{NULL} to skip writing files.}

\item{lfcCutoff, pCutoff}{Numeric cutoffs passed through to the plots that use them.}

\item{palette}{Character vector of group colours.}

\item{n_heatmap}{Integer. Number of genes in the DEG heatmap.}

\item{n_correlated}{Integer. Number of genes in the co-expression plot.}

\item{verbose}{Logical. Report progress with \code{message()}.}
}
\value{
\code{res_output} with the extra plots (and their \verb{*Path} entries) added.
}
\description{
Runs the QC and exploration plots on an existing \code{res_output}, adds them to
the list, and optionally writes them to disk. Each plot is attempted
independently: one failure never aborts the rest.
}
\details{
This is called for you at the end of \code{\link[=pipeline]{pipeline()}}, but it is also the cheap
way to redraw the figures (with different cutoffs, colours or gene counts)
from a run you already have in memory.
}
\examples{
\dontrun{
res <- run_pipeline(input = "counts_dir", dataType = "counts", goi = "TP53")
res <- build_extra_plots(res, n_heatmap = 60)
res$PCAplot
}
}
