% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_res_df}
\alias{get_res_df}
\title{Get annotated DESeq2 results}
\usage{
get_res_df(dds, rowNamesOfCounts, mart, pCutoff = 0.05, pAdjustMethod = "BH")
}
\arguments{
\item{dds}{A \code{DESeqDataSet} object after running \code{DESeq()}}

\item{rowNamesOfCounts}{Character. The column in your annotation dataset (e.g. \code{"external_gene_name"})
used to match against the DESeq results row names.}

\item{mart}{A \code{biomaRt} Mart object (created with \code{useMart()}) for annotation lookup.}

\item{pCutoff}{Numeric scalar. The \code{alpha} threshold for adjusted p-values (default \code{0.05}).}

\item{pAdjustMethod}{Character. Method for p-value adjustment (default \code{"BH"}).}
}
\value{
A \code{data.frame} with one row per gene, including all \code{results()} columns
plus annotation fields (\code{external_gene_name}, \code{entrezgene_id}, etc.), sorted by \code{padj}.
}
\description{
Fetches \code{results()} from a \code{DESeqDataSet}, adds a “significant” flag,
and joins in external gene annotations via biomaRt.
}
\examples{
\dontrun{
library(DESeq2); library(biomaRt)
mart <- useMart("ensembl", dataset = "hsapiens_gene_ensembl")
dds <- DESeqDataSetFromMatrix(countData = counts, colData = coldata, design = ~ condition)
dds <- DESeq(dds)
df   <- get_res_df(dds, row_key = "ensembl_gene_id", mart = mart)
head(df)
}
}
