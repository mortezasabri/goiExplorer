% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_deg_heatmap}
\alias{plot_deg_heatmap}
\title{Heatmap of the top differentially expressed genes}
\usage{
plot_deg_heatmap(
  res_output,
  n = 40,
  goi = res_output$goi,
  highlight_colour = "#c0392b"
)
}
\arguments{
\item{res_output}{The list returned by \code{\link[=run_pipeline]{run_pipeline()}} / \code{\link[=pipeline]{pipeline()}}.}

\item{n}{Integer. Number of top genes (ranked by adjusted p-value).}

\item{goi}{Character. Gene to highlight; defaults to the pipeline's GOI.}

\item{highlight_colour}{Colour used for the GOI label.}
}
\value{
A ggplot object, or \code{NULL} when the required pieces are missing.
}
\description{
Row z-scores of the variance-stabilised counts for the \code{n} most significant
genes, clustered on both axes. The gene of interest is always included and
its label is highlighted, so you can see immediately whether it behaves like
the rest of the signature.
}
\examples{
\dontrun{
plot_deg_heatmap(res, n = 30)
}
}
