% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_goi_rank}
\alias{plot_goi_rank}
\title{Where the gene of interest ranks among all genes}
\usage{
plot_goi_rank(res_output, goi = res_output$goi, lfcCutoff = 1, pCutoff = 0.05)
}
\arguments{
\item{res_output}{The list returned by \code{\link[=run_pipeline]{run_pipeline()}} / \code{\link[=pipeline]{pipeline()}}.}

\item{goi}{Character. Gene to highlight; defaults to the pipeline's GOI.}

\item{lfcCutoff}{Numeric. Fold-change cutoff drawn as guide lines.}

\item{pCutoff}{Numeric. Adjusted p-value cutoff used to colour significance.}
}
\value{
A ggplot object, or \code{NULL} when results are missing.
}
\description{
Every gene ordered by log2 fold change, with the GOI marked. The subtitle
reports its rank and percentile, which answers the very first question people
ask about a GOI: "is this actually one of the strongest changes, or just one
that happens to be significant?"
}
\examples{
\dontrun{
plot_goi_rank(res)
}
}
