% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_pca}
\alias{plot_pca}
\title{PCA of the samples}
\usage{
plot_pca(
  res_output,
  ntop = 500,
  intgroup = "condition",
  palette = c("#4f8832", "#f79c18"),
  label_samples = TRUE
)
}
\arguments{
\item{res_output}{The list returned by \code{\link[=run_pipeline]{run_pipeline()}} / \code{\link[=pipeline]{pipeline()}}.}

\item{ntop}{Integer. Number of most variable genes to use (default 500).}

\item{intgroup}{Character. Column of \code{colData} used for colouring.}

\item{palette}{Character vector of fill colours, one per group.}

\item{label_samples}{Logical. Draw sample names next to the points.}
}
\value{
A ggplot object, or \code{NULL} if no variance-stabilised data is available.
}
\description{
Principal component analysis on the variance-stabilised counts, using the
\code{ntop} most variable genes. The percentage of variance explained is printed
on each axis, which makes it easy to see whether the experimental groups are
actually the main source of variation in the data.
}
\examples{
\dontrun{
res <- run_pipeline(input = "counts_dir", dataType = "counts", goi = "TP53")
plot_pca(res)
}
}
