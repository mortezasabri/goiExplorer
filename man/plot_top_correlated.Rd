% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_top_correlated}
\alias{plot_top_correlated}
\title{Genes co-expressed with the gene of interest}
\usage{
plot_top_correlated(res_output, n = 20, goi = res_output$goi, min_var = 0)
}
\arguments{
\item{res_output}{The list returned by \code{\link[=run_pipeline]{run_pipeline()}} / \code{\link[=pipeline]{pipeline()}}.}

\item{n}{Integer. Number of genes to show.}

\item{goi}{Character. Gene to correlate against; defaults to the pipeline's GOI.}

\item{min_var}{Numeric. Genes with variance at or below this value are dropped.}
}
\value{
A ggplot object, or \code{NULL} when the GOI is absent from the matrix.
The underlying table is attached as the \code{"data"} attribute.
}
\description{
Pearson correlation of every gene against the GOI across all samples, on the
variance-stabilised scale. Useful as a cheap, annotation-free way to find
candidate partners of the GOI in this particular dataset.
}
\details{
Note that with few samples and two clearly separated groups the top hits are
largely genes that follow the same group split, so treat this as a
hypothesis generator rather than evidence of co-regulation.
}
\examples{
\dontrun{
p <- plot_top_correlated(res, n = 25)
head(attr(p, "data"))
}
}
