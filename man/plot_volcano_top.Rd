% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_volcano_top}
\alias{plot_volcano_top}
\title{Volcano plot labelling the strongest genes}
\usage{
plot_volcano_top(
  res_output,
  n_labels = 15,
  goi = res_output$goi,
  lfcCutoff = 1,
  pCutoff = 0.05
)
}
\arguments{
\item{res_output}{The list returned by \code{\link[=run_pipeline]{run_pipeline()}} / \code{\link[=pipeline]{pipeline()}}.}

\item{n_labels}{Integer. Number of top genes (by adjusted p-value) to label.}

\item{goi}{Character. Gene to always label; defaults to the pipeline's GOI.}

\item{lfcCutoff}{Numeric. Fold-change cutoff.}

\item{pCutoff}{Numeric. Adjusted p-value cutoff.}
}
\value{
A ggplot object, or \code{NULL} when results are missing.
}
\description{
Companion to the volcano plot the pipeline already produces: instead of
labelling only the GOI, this one also names the most significant genes, so
the plot works as a standalone summary figure.
}
\examples{
\dontrun{
plot_volcano_top(res, n_labels = 20)
}
}
