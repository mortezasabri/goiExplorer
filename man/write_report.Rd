% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{write_report}
\alias{write_report}
\title{Write a single-file HTML report for a run}
\usage{
write_report(res_output, file = NULL, n_genes = 50, ai = FALSE, ...)
}
\arguments{
\item{res_output}{The list returned by \code{\link[=run_pipeline]{run_pipeline()}} / \code{\link[=pipeline]{pipeline()}}.}

\item{file}{Character. Output path. Defaults to \verb{goiExplorer_report_<GOI>.html}
inside the run's output directory.}

\item{n_genes}{Integer. Number of DE genes to include in the table.}

\item{ai}{Logical. Also ask the LLM for an interpretation and include it.
Requires an API key; see \code{\link[=ai_agent]{ai_agent()}}. Failures are reported in the report
rather than raised.}

\item{...}{Passed to \code{\link[=ai_interpret]{ai_interpret()}} when \code{ai = TRUE}.}
}
\value{
The path to the written file, invisibly.
}
\description{
Collects the numbers, the figures and the annotation tables from a finished
run into one self-contained HTML file you can email, attach to a lab
notebook, or open months later without re-running anything. Figures are
embedded directly in the file when the knitr package is available.
}
\examples{
\dontrun{
res <- run_pipeline(input = "counts_dir", dataType = "counts", goi = "TP53")
write_report(res)
write_report(res, ai = TRUE) # with an LLM interpretation
}
}
